# Supabase connection for the WTT Value Simulator.
# The URL and publishable key are also embedded in supabase-client.js so the
# static HTML works when opened directly; this file documents them and holds
# the secrets used only for administration (never ship the secret/service key).

SUPABASE_URL=https://oldfpsvbmwhkbtfrzmdi.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_ZJ1eHnFmU25TZ__oQSYjWA_6CkM5LXU

# Needed only to run `supabase db push` / manage the project — do NOT commit a real value.
# SUPABASE_ACCESS_TOKEN=
# SUPABASE_DB_PASSWORD=
